export interface CommutersDTO {
  ags: string;
  districtName: string;
  commutersIn: number | null;
  commutersOut: number | null;
  commuterBalance: number | null;
  commuterRatio: number | null;
  dataYear: number | null;
}
